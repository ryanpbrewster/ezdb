{
  "queries": [{
    "name": "all",
    "raw_sql": "SELECT * FROM person"
  }, {
    "name": "get",
    "raw_sql": "SELECT * FROM person WHERE id = :id"
  }],
  "mutations": [{
      "name": "add",
      "raw_sql": "INSERT INTO person (name) VALUES (:name)"
  }]
}
